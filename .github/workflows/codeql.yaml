---
name: codeql

# CodeQL runs from the shared thingzio/actions workflow. The visibility gate,
# the explicit build, and the vendoring handling all live there now -- see
# https://github.com/thingzio/actions/blob/main/.github/workflows/codeql-go.md

on:
  schedule:
    - cron: '0 5 * * 1'
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  analyze:
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: thingzio/actions/.github/workflows/codeql-go.yaml@7276775800dbfef1d9671e60c337f3b4b479a4e8  # v1.8.0
